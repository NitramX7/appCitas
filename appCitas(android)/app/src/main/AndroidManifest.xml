<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCitas"
        android:usesCleartextTraffic="true">

        <!-- Actividades de la aplicación -->
        <activity android:name="pantallas.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="pantallas.Pantalla1" android:exported="false" />
        
        <!-- AÑADIDA LA DECLARACIÓN QUE FALTABA -->
        <activity android:name="pantallas.CrearCita" android:exported="false" />

        <activity android:name="pantallas.MisCitas" android:exported="false" />
        <activity android:name="pantallas.EditarCita" android:exported="false" />

    </application>

</manifest>